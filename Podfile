# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MoviesAppV2.0' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!:linkage => :static

  # Pods for MoviesAppV2.0
	pod 'Alamofire'
  	pod 'SDWebImage', '~> 5.0'
	pod 'Cosmos', '~> 23.0'

  target 'MoviesAppV2.0Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MoviesAppV2.0UITests' do
    # Pods for testing
  end

end
